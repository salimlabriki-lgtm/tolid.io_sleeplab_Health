{
  "name": "sleeplab-dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "pip install -U pip && pip install -r etl/requirements.txt || true",
  "customizations": {
    "vscode": { "extensions": ["ms-python.python"] }
  }
}
